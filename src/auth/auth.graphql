scalar Password
type Auth {
  user: User!
  token: String!
}

input SignUpInput {
  email: String!
  password: Password!
  confirmPassword: String!
  firstName: String!
  lastName: String!
}

input SignInInput {
  email: String!
  password: Password!
}

type Mutation {
  signUp(signUpInput: SignUpInput!): Auth!
  signIn(signInInput: SignInInput!): Auth!
}
